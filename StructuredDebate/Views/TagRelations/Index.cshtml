@model IEnumerable<StructuredDebate.Models.TagRelation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Post.Claim)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tag.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Post.Claim)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tag.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TagRelationID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TagRelationID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TagRelationID })
        </td>
    </tr>
}

</table>
